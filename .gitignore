# Heavy raw inputs — LDD land-use + downloaded admin boundaries (prep-only)
raw/

# Intermediate pipeline outputs (regenerable): GeoPackages and quick-look maps.
# work/*_fields.geojson and work/*_summary.csv stay tracked (small, useful history).
work/*.gpkg
work/*_map.html

# NOTE: app/ is the deployable web app — keep it tracked (incl. app/th_adm3.geojson,
# which the app fetches at runtime, and app/data/*.geojson).

# Secrets — local environment (FIRMS_MAP_KEY etc.). Never commit. See .env.example.
.env

# Python / OS / editor cruft
__pycache__/
*.pyc
.DS_Store
Thumbs.db
